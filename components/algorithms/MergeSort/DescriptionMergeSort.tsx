import React from "react";

const DescriptionMergeSort: React.FC = () => (
  <p className="text-lg">
    Merge Sort is a divide-and-conquer algorithm that divides the array into
    halves, sorts each half recursively, and then merges them into a single
    sorted array.
  </p>
);

export default DescriptionMergeSort;
